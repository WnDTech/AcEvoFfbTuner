<Window x:Class="AcEvoFfbTuner.Views.FeedbackDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Send Diagnostics — Feedback Required"
        Width="480"
        Height="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BgDark}"
        Foreground="{StaticResource TextBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Describe any issues or feedback before sending:" FontSize="14" FontWeight="Bold" Margin="0,0,0,6" />

        <TextBlock Grid.Row="1" FontSize="12" Foreground="#FF888888" Margin="0,0,0,10" TextWrapping="Wrap">
            <Run Text="What felt wrong? Where on track? What wheel base? This gets sent with your diagnostic data." />
        </TextBlock>

        <TextBox Grid.Row="2" x:Name="FeedbackBox" FontSize="13" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" VerticalContentAlignment="Top" Padding="8,6" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Send" Width="90" Margin="0,0,10,0" Padding="16,6" Click="OnSend" IsDefault="True" />
            <Button Content="Cancel" Width="90" Padding="16,6" Style="{StaticResource OutlinedButton}" Click="OnCancel" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
